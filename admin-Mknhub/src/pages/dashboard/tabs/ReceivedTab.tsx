import React from "react";
import { Card, CardContent, Typography } from "@mui/material";

export const ReceivedTab: React.FC = () => {
  return (
    <Card>
      <CardContent>
        <Typography variant="h6">Tin nhận được</Typography>
      </CardContent>
    </Card>
  );
};

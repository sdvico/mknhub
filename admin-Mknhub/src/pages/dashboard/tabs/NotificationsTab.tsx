import React from "react";
import { Card, CardContent, Typography } from "@mui/material";

export const NotificationsTab: React.FC = () => {
  return (
    <Card>
      <CardContent>
        <Typography variant="h6">Thống kê Thông báo</Typography>
      </CardContent>
    </Card>
  );
};
